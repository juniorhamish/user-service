To create a new migration:
1. Run the command `supabase migration new <migration_name>` to generate a new migration file.
2. Open the generated migration file and add the required SQL statements.
3. Run `supabase db reset --linked` to completely reset the database.
